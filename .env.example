# Copy to .env and fill in. NEVER commit .env.
# OpenAI API key — used by sdg_hub (data generation teacher) and its_hub (BoN judge + ceiling row)
OPENAI_API_KEY=sk-...

# Optional: HuggingFace token if you push fine-tuned adapters to the Hub
# HF_TOKEN=hf_...
